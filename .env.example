# Required: Gemini API Key (get from https://aistudio.google.com/apikey)
GEMINI_API_KEY=your_api_key_here

# Optional: PostHog Analytics (for LLM observability and debugging)
# Get keys from your PostHog project settings
# POSTHOG_API_KEY=phc_...
# POSTHOG_HOST=https://us.i.posthog.com  # or https://eu.i.posthog.com

# Optional Configuration
# =====================

# Cache TTL (default: 360 hours = 15 days)
CACHE_MAX_AGE_HOURS=360

# Concurrency Settings
# Model-specific recommendations based on rate limits:
# - Gemini 2.5 Flash (1,000 RPM): Use 30-60 for maximum speed
# - Gemini 2.5 Pro (150 RPM): Use 10-20 (more conservative)
# - Free Tier (10 RPM): Use 3-5
CATEGORIZER_CONCURRENCY=40            # Default: 40 (optimized for Flash paid tier)

# Gemini Model
# - gemini-2.5-flash: Fast & cheap (~2s per repo, RECOMMENDED)
#   * Thinking is disabled (thinkingBudget: 0) for speed
#   * Perfect for simple categorization tasks
# - gemini-2.5-pro: Slower but uses reasoning (~15s per repo)
#   * Cannot disable thinking - always uses reasoning tokens
#   * Overkill for categorization - use Flash instead
GEMINI_MODEL=gemini-2.5-flash

# Retry Configuration
# Flash is fast - use shorter delays for faster recovery from rate limits
RETRY_DELAY_MS=1000                   # Initial retry delay (default: 1000ms)
RETRY_MAX_ATTEMPTS=5                  # Max retry attempts (default: 5)
